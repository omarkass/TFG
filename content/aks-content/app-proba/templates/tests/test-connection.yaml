apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-proba.fullname" . }}-test-connection"
  labels:
    {{- include "app-proba.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-proba.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
